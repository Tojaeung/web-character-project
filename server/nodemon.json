{
  "watch": ["src"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "--exec ./node_modules/.bin/ts-node -r tsconfig-paths/register --files ./src/server.ts",
  "restartable": "r5",
  "env": {
    "NODE_ENV": "development"
  }
}
